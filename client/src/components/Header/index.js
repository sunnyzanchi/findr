import { h } from 'preact';
import styled from 'astroturf'

const Header = styled('h1')`
  background: rgb(243, 162, 26);
  color: #fff;
  margin: 0;
  padding: 12px;
  text-align: center;
`

export default Header
